# Smoke Test — 5 facts to verify the benchmark pipeline works end-to-end.
# Run with: poetry run python -m benchmark.run --input benchmark/smoke_test.md --run-id smoke

F01: Marie Curie was born in 1867 in Warsaw.
F02: LangGraph is a Python framework for building stateful AI agents.
F03: Qdrant is a vector database optimised for nearest-neighbour search.
F04: Git was created by Linus Torvalds in 2005.
F05: Redis can be used as an in-memory database, message broker, and cache.
